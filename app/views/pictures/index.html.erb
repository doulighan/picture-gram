<h1> Most Recent Pictures </h1>
<hr>
<% @pictures.each do |pic| %>
  <% if pic.image_url != nil %>
    <%= link_to image_tag(pic.image_url, size: "295x295"), picture_path(pic) %> 
  <% else %> 
    <%= link_to pic.title, picture_path(pic) %>
  <% end %>
<% end %>