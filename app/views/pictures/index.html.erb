<h1> Most Recent Pictures </h1>
<hr>
<% @pictures.each do |pic| %>

<%= link_to image_tag(pic.image_url), picture_path(pic) %>
<ul>
<% pic.tags.all.each do |tag| %>
 <li>  <%= tag.name %> </li>
  <% end %>
  </ul>
<% end %>