<h1> Most Recent Pictures </h1>
<hr>
<% @pictures.each do |pic| %>

<span> <%= link_to image_tag(pic.image_url), picture_path(pic) %> </span>
<ul>
<% pic.tags.all.each do |tag| %>
 <%= tag.name %>
  <% end %>
  </ul>
<% end %>